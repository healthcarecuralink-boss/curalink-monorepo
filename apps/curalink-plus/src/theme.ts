import { createThemeProvider, curalinkPlusColors, curalinkPlusRadius, curalinkPlusType } from "@curalink/ui";

export const ThemeProvider = createThemeProvider({
  colors: curalinkPlusColors,
  radius: curalinkPlusRadius,
  type: curalinkPlusType,
});
